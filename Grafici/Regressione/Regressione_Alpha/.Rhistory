p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1.2, tau = 3,Offset = 0.01),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1.2, tau = 3,Offset = 0.1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1.2, tau = 7,Offset = 0.1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1, tau = 7,Offset = 0.1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 2, tau = 7,Offset = 0.1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 2, tau = 7,Offset = 1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 7,Offset = 1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~TIME_us,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~TIME_us,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~t_rel,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~t_rel,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
dfs <- dfs %>% filter(t_rel > 0)
dfm <- dfm %>% filter(t_rel > 0)
# Creazione del grafico nativo con sovrapposizione corretta
p_plotly <- plot_ly() %>%
# Traccia 1
add_lines(data = dfm,
x = ~t_rel,
y = ~CH1,
name = 'SiPM con partielle alpha',
line = list(color = "black", width = 1.5)) %>%
# Traccia 2
add_lines(data = dfs,
x = ~t_rel,
y = ~CH1,
name = 'Simulazione',
line = list(color = 'red', width = 1.5)) %>%
# Layout e titoli
layout(
title = list(
text = paste0("<b>Confronto Impulsi simulazione vs misurazione</b><br>")
),
xaxis = list(title = "Tempo [\u03bc s]", gridcolor = "#f0f0f0"),
yaxis = list(title = "Ampiezza [V]", gridcolor = "#f0f0f0"),
legend = list(
orientation = 'h',
x = 0.5,
xanchor = 'center',
y = -0.35
),
margin = list(t = 80, b = 120),
plot_bgcolor = "white"
)
# Visualizza il grafico
p_plotly
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
fitm
fits
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
fitm$tau
fits
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
fitm$call
fits
fitm
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 0.5, tau = 5,Offset = 1),
control = nls.control(maxiter = 1000))
fits
fits <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfs,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fitm <- nlsLM(CH1 ~ A * exp(-t_rel / tau) + Offset,
data = dfm,
start = list(A = 1.2, tau = 5,Offset = 0.01),
control = nls.control(maxiter = 1000))
fits
fitm
coef(fitm)
taus = coef(fits)$tau
taus = coef(fits)
taus = coef(fits)
taus$tau
taus = coef(fits)
taus
taus = coefficients(fits)
taus
taus = coefficients(fits)
taus$tau
taus = coefficients(fits)
taus.tau
taus = coefficients(fits)
taus
taus = coefficients(fits)["tau"]
taus
taus = coef(fits)["tau"]
taus
taus = coef(fits)["tau"]
as.numeric(taus)
taus = as.numeric(coef(fits)["tau"])
taus
taus = as.numeric(coef(fits)["tau"])
taum = as.numeric(coef(fitm)["tau"])
dif = (abs(taus-taum)/taum)*100
dif
